# Runtime dependencies only.
# For dev deps (pytest, httpx) install via: pip install -e ".[dev]"
meshtastic>=2.3.0
paho-mqtt>=2.0.0
PyYAML>=6.0
textual>=0.50.0
rich>=13.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
